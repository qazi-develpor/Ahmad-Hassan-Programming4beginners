#include <stdio.h>
#include <stdlib.h>

int main() {
    int num;
    if (scanf("%d", &num) != 1) {
        return 0;
    }

    int *arr = malloc(num * sizeof(int));
    if (arr == NULL) {
        return 0;
    }

    for (int i = 0; i < num; i++) {
        scanf("%d", &arr[i]);
    }

    /* Reverse logic */
    int left = 0;
    int right = num - 1;
    while (left < right) {
        int temp = arr[left];
        arr[left] = arr[right];
        arr[right] = temp;
        left++;
        right--;
    }

    for (int i = 0; i < num; i++) {
        printf("%d", arr[i]);
        if (i != num - 1) {
            printf(" ");
        }
    }
    printf("\n");

    free(arr);
    return 0;
}
